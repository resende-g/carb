# Handoff da implementação v1.3

**Data:** 2026-08-30
**Branch:** `v1.3`, criada a partir de `v1.2` sincronizada
**Estado:** implementação, backend remoto e hospedagem pública concluídos.

## Implementado

- Supabase JS, cliente público e fallback explícito para fixtures.
- Auth administrativo individual, TOTP/AAL2, RBAC e autorização editorial por perfil.
- Migrations de schema, RLS/RPCs, Storage privado, seed sintético e teste pgTAP.
- Posts, revisões, hashtags, documentos, reações, dashboard, auditoria e sucessão.
- Edge Functions `admin-auth` e `admin-users`; bootstrap reproduzível do primeiro SUPERADMIN.
- Painel administrativo responsivo e documentação v1.3.
- Versão atualizada para `1.3.0` em `package.json` e `package-lock.json`.

## Evidências executadas

- `npm run lint`: passou.
- `npm run typecheck`: passou.
- `npm test -- --run`: 9 arquivos e 20 testes passaram.
- `npm run build`: passou; único aviso é chunk JS de 636,80 kB acima de 500 kB.
- `npx supabase db reset --local`: três migrations e seed aplicados em PostgreSQL 17.6.1.
- `npx supabase test db --local`: 18 testes pgTAP passaram.
- `npx supabase db advisors --local --type all --level info --fail-on error`: nenhum erro; apenas índices sem uso no banco recém-criado, duas duplicidades de policies permissivas e acesso direto intencionalmente ausente em `reactions`.
- `git diff --check`: passou.
- Cloudflare: `/`, `/admin`, asset JS e PDF retornaram 200; fallback SPA, cache e cabeçalhos de segurança verificados.
- Supabase remoto: três migrations e seed aplicados; 13 tabelas sob RLS; duas Edge Functions ativas na versão 2; CORS verificado com 204.
- Bundle publicado contém `https://sigaa.ufba.br/sigaa/public/home.jsf`.
- Verificação manual anterior: desktop 1280×800 e mobile 390×844 sem overflow ou erros de console; menu, tema, navegação, busca e fallback do login verificados.
- Busca de segredos: nenhum valor de chave encontrado; apenas nomes de variáveis e documentação histórica.

## Publicação

- URL: https://carb-v1-3.portal-carb-prototipo.workers.dev
- Cloudflare Workers Static Assets com fallback SPA e headers de segurança.
- Supabase remoto conectado ao frontend apenas por URL pública e chave publicável.

## Commits da entrega

- `1d9b074 feat(supabase): add persistent backend and security model`
- `3d7c2ea feat(editorial): integrate auth and administrative workflow`
- `docs(v1.3): document deployment and governance`
- `feat(deploy): host v1.3 on Cloudflare Workers`

## Pendências operacionais

1. Configurar SMTP e redirects do Supabase Auth.
2. Executar o bootstrap do primeiro `SUPERADMIN` com dados pessoais fornecidos diretamente ao ambiente seguro, nunca ao repositório.
3. Validar cada papel administrativo com AAL2 antes de liberar o painel.
4. Manter a classificação de protótipo; a publicação técnica não equivale a homologação institucional.

## Serviço remoto conhecido

O projeto Supabase consultado estava `ACTIVE_HEALTHY`, em PostgreSQL 17.6 e região `ca-central-1`. Nenhuma credencial foi gravada no repositório ou neste arquivo.
